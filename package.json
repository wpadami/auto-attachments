{
    "name": "auto-attachments",
    "version": "0.14.0",
    "description": "Admin settings UI and block editor build for the Auto Attachments WordPress plugin.",
    "private": true,
    "scripts": {
        "build": "npm run build:admin && npm run build:block",
        "build:admin": "wp-scripts build src/admin/index.js --output-path=build/admin",
        "build:block": "wp-scripts build src/block/index.js --output-path=build/block && cp src/block/block.json build/block/block.json",
        "start": "wp-scripts start src/admin/index.js --output-path=build/admin",
        "lint:js": "wp-scripts lint-js src"
    },
    "devDependencies": {
        "@wordpress/scripts": "^30.0.0"
    }
}
